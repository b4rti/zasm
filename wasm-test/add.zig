export const nice: i32 = 69;
export const blaze_it = [_]u8{ '4', '2', '0' };

export fn add(a: i32, b: i32) i32 {
    const x = 1;
    var y: u8 = 2;
    const z = x + y;
    return a + b + nice + z;
}
